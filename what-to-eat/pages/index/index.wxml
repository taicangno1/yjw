<view class="container">
  <view class="header">
    <text class="logo-icon">🍜</text>
    <text class="title">今天吃什么</text>
    <text class="subtitle">翻一翻，发现附近美食</text>
  </view>

  <view class="location-card">
    <view class="location-header">
      <text class="location-icon">📍</text>
      <text class="location-label">当前位置</text>
    </view>
    <view class="location-info">
      <text class="location-text">{{locationText}}</text>
      <text class="reget-btn" bindtap="getLocation">重新定位</text>
    </view>
  </view>

  <view class="range-section">
    <text class="section-title">选择配送范围</text>
    <range-picker 
      ranges="{{ranges}}" 
      selected="{{selectedRange}}" 
      bind:select="onRangeSelect" 
    />
  </view>

  <button 
    class="start-btn {{canStart ? '' : 'disabled'}}" 
    bindtap="startChoosing"
    disabled="{{!canStart}}"
  >
    开始选餐
  </button>
</view>